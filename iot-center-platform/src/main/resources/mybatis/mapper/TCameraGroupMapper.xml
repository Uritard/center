<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjh.platform.module.user.dao.TCameraGroupDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yjh.platform.module.user.entity.TCameraGroup">
        <id column="group_id" jdbcType="BIGINT" property="groupId" />
        <result column="group_name" jdbcType="VARCHAR" property="groupName" />
        <result column="camera_ids" jdbcType="VARCHAR" property="cameraIds" />
        <result column="remarks" jdbcType="VARCHAR" property="remarks" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        group_id, group_name, camera_ids, remarks
    </sql>


    <insert id="add" useGeneratedKeys="true" keyProperty="groupId" parameterType="com.yjh.platform.module.user.entity.TCameraGroup">
        insert into t_camera_group(
            group_id,
            group_name,
            camera_ids,
            remarks
        )values(
                #{groupId, jdbcType=BIGINT},
                #{groupName, jdbcType=VARCHAR},
                #{cameraIds, jdbcType=VARCHAR},
                #{remarks, jdbcType=VARCHAR}
        )
    </insert>

    <delete id="deleteByPrimaryId" parameterType="java.lang.Long">
        delete from t_camera_group
                where group_id = #{groupId, jdbcType=BIGINT}
    </delete>

    <update id="update" parameterType="com.yjh.platform.module.user.entity.TCameraGroup">
        update t_camera_group a <set>
        <if test="groupId != null and groupId != ''">a.group_id = #{groupId, jdbcType=BIGINT},</if>
        <if test="groupName != null and groupName != ''">a.group_name = #{groupName, jdbcType=VARCHAR},</if>
        <if test="cameraIds != null and cameraIds != ''">a.camera_ids = #{cameraIds, jdbcType=VARCHAR},</if>
        <if test="remarks != null and remarks != ''">a.remarks = #{remarks, jdbcType=VARCHAR},</if>
    </set>
            where a.group_id = #{groupId, jdbcType=BIGINT}
    </update>

    <select id="selectByPrimaryId" parameterType="java.lang.Long" resultType="com.yjh.platform.module.user.entity.TCameraGroup">
        select
        a.group_id,
        a.group_name,
        a.camera_ids,
        a.remarks
        from t_camera_group a
        where 1 = 1
            and a.group_id = #{groupId, jdbcType=BIGINT}
        limit 1
    </select>

    <select id="select" parameterType="com.yjh.platform.module.user.entity.TCameraGroup" resultType="com.yjh.platform.module.user.entity.TCameraGroup">
        select
        a.group_id,
        a.group_name,
        a.camera_ids,
        a.remarks
        from t_camera_group a
        where 1 = 1
        <if test="groupId != null and groupId != ''"> and a.group_id = #{groupId, jdbcType=BIGINT}</if>
        <if test="groupName != null and groupName != ''"> and a.group_name = #{groupName, jdbcType=VARCHAR}</if>
        <if test="cameraIds != null and cameraIds != ''"> and a.camera_ids = #{cameraIds, jdbcType=VARCHAR}</if>
        <if test="remarks != null and remarks != ''"> and a.remarks = #{remarks, jdbcType=VARCHAR}</if>
    </select>

    <select id="selectByPage" parameterType="com.yjh.platform.module.user.entity.TCameraGroup" resultType="com.yjh.platform.module.user.entity.TCameraGroup">
        select
        a.group_id,
        a.group_name,
        a.camera_ids,
        a.remarks
        from t_camera_group a
        where 1 = 1
        <if test="groupId != null and groupId != ''"> and a.group_id = #{groupId, jdbcType=BIGINT}</if>
        <if test="groupName != null and groupName != ''"> and a.group_name = #{groupName, jdbcType=VARCHAR}</if>
        <if test="cameraIds != null and cameraIds != ''"> and a.camera_ids = #{cameraIds, jdbcType=VARCHAR}</if>
        <if test="remarks != null and remarks != ''"> and a.remarks = #{remarks, jdbcType=VARCHAR}</if>
    </select>

    <insert id="batchAdd">
        insert into t_camera_group(
            group_id,
            group_name,
            camera_ids,
            remarks
        )values
        <foreach collection="list" item="TCameraGroup" separator=",">
            (
                #{TCameraGroup.groupId, jdbcType=BIGINT},
                #{TCameraGroup.groupName, jdbcType=VARCHAR},
                #{TCameraGroup.cameraIds, jdbcType=VARCHAR},
                #{TCameraGroup.remarks, jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

    <delete id="batchDelete">
        delete from t_camera_group
        where group_id in (
            <foreach collection="list" item="groupId" separator=",">
                #{groupId, jdbcType=BIGINT}
            </foreach>
            )
    </delete>

    <select id="selectIsExit" resultType="com.yjh.platform.module.user.entity.TCameraGroup">
         select
        a.group_id,
        a.group_name,
        a.camera_ids,
        a.remarks
        from t_camera_group a
        where  a.group_name = #{groupName, jdbcType=VARCHAR}
    </select>

    <select id="selectAll" resultType="com.yjh.platform.module.user.entity.TCameraGroup">
        select *
        from t_camera_group
    </select>

</mapper>
