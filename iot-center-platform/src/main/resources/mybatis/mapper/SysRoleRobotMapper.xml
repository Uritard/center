<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjh.platform.module.user.dao.SysRoleRobotDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yjh.platform.module.user.entity.SysRoleRobot">
        <id column="role_id" jdbcType="BIGINT" property="roleId" />
        <result column="robot_id" jdbcType="BIGINT" property="robotId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        role_id, robot_id
    </sql>


    <insert id="insert" useGeneratedKeys="true" keyProperty="roleId" parameterType="com.yjh.platform.module.user.entity.SysRoleRobot">
        insert into sys_role_robot(
            role_id,
            robot_id
        )values(
                #{roleId, jdbcType=BIGINT},
                #{robotId, jdbcType=BIGINT}
        )
    </insert>

    <delete id="deleteByPrimaryId" parameterType="java.lang.Long">
        delete from sys_role_robot
                where role_id = #{roleId, jdbcType=BIGINT}
    </delete>

    <update id="update" parameterType="com.yjh.platform.module.user.entity.SysRoleRobot">
        update sys_role_robot a <set>
        <if test="roleId != null">a.role_id = #{roleId},</if>
        <if test="robotId != null ">a.robot_id = #{robotId},</if>
    </set>
            where a.role_id = #{roleId, jdbcType=BIGINT}
    </update>

    <select id="selectByPrimaryId" parameterType="java.lang.Long" resultType="com.yjh.platform.module.user.entity.SysRoleRobot">
        select
        a.role_id,
        a.robot_id
        from sys_role_robot a
        where 1 = 1
            and a.role_id = #{roleId, jdbcType=BIGINT}
        limit 1
    </select>

    <select id="select" parameterType="com.yjh.platform.module.user.entity.SysRoleRobot" resultType="com.yjh.platform.module.user.entity.SysRoleRobot">
        select
        a.role_id,
        a.robot_id
        from sys_role_robot a
        where 1 = 1
        <if test="roleId != null and roleId != ''"> and a.role_id = #{roleId, jdbcType=BIGINT}</if>
        <if test="robotId != null and robotId != ''"> and a.robot_id = #{robotId, jdbcType=BIGINT}</if>
    </select>

    <select id="selectByPage" parameterType="com.yjh.platform.module.user.entity.SysRoleRobot" resultType="com.yjh.platform.module.user.entity.SysRoleRobot">
        select
        a.role_id,
        a.robot_id
        from sys_role_robot a
        where 1 = 1
        <if test="roleId != null and roleId != ''"> and a.role_id = #{roleId, jdbcType=BIGINT}</if>
        <if test="robotId != null and robotId != ''"> and a.robot_id = #{robotId, jdbcType=BIGINT}</if>
    </select>

    <insert id="batchInsert">
        insert into sys_role_robot(
        role_id,
        robot_id
        )values
        <foreach collection="list" item="SysRoleRobot" separator=",">
            (#{SysRoleRobot.roleId, jdbcType=BIGINT},
            #{SysRoleRobot.robotId, jdbcType=BIGINT})
        </foreach>
    </insert>

</mapper>
