<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjh.accessmeter.module.dao.TIotDeviceDao">

    <resultMap id="BaseResultMap" type="com.yjh.accessmeter.module.device.entity.IotDevice">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="deviceName" column="device_name" jdbcType="VARCHAR"/>
        <result property="ip" column="ip" jdbcType="VARCHAR"/>
        <result property="port" column="port" jdbcType="INTEGER"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="iotDeviceType" column="iot_device_type" jdbcType="INTEGER"/>
        <result property="protocolModel" column="protocol_model" jdbcType="VARCHAR"/>
        <result property="deviceId" column="device_id" jdbcType="VARCHAR"/>
        <result property="upRegionId" column="up_region_id" jdbcType="BIGINT"/>
        <result property="upRegionName" column="up_region_name" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="createPerson" column="create_person" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="updatePerson" column="update_person" jdbcType="VARCHAR"/>
        <result property="edgeCode" column="edge_code" jdbcType="VARCHAR"/>
        <result property="magnificationCoefficient" column="magnification_coefficient" jdbcType="FLOAT"/>
        <result property="collectionFrequency" column="collection_frequency" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,device_name,ip,port,address,iot_device_type,protocol_model,device_id,up_region_id,up_region_name,create_time,
        create_person,update_time,update_person,edge_code,magnification_coefficient,collection_frequency
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_iot_device
    </select>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_iot_device where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectPointByDeviceId" resultType="com.yjh.accessmeter.module.device.entity.IotDevicePoint">
        select id,iot_device_id,iot_device_name,channel_num,point_name,unit,extend from t_iot_device_point where iot_device_id = #{deviceId, jdbcType=BIGINT}
    </select>

    <insert id="insertData" useGeneratedKeys="true" keyProperty="id" parameterType="com.yjh.accessmeter.module.device.entity.IotDeviceData">
        insert into t_iot_device_data(
            id,
            point_id,
            point_name,
            iot_device_id,
            iot_device_name,
            value,
            unit,
            up_region_id,
            create_time,
            magnification_coefficient,
            edge_code,
            iot_device_type
        )values(
            #{id, jdbcType=BIGINT},
            #{pointId, jdbcType=BIGINT},
            #{pointName, jdbcType=VARCHAR},
            #{iotDeviceId, jdbcType=BIGINT},
            #{iotDeviceName, jdbcType=VARCHAR},
            #{value, jdbcType=VARCHAR},
            #{unit, jdbcType=VARCHAR},
            #{upRegionId, jdbcType=BIGINT},
            #{createTime, jdbcType=TIMESTAMP},
            #{item.magnificationCoefficient, jdbcType=FLOAT},
            #{item.edgeCode, jdbcType=VARCHAR},
            #{item.iotDeviceType, jdbcType=INTEGER}
        )
    </insert>

    <insert id="batchInsertData" useGeneratedKeys="true" keyProperty="id" parameterType="com.yjh.accessmeter.module.device.entity.IotDeviceDataEx">
        insert into t_iot_device_data (
        id,
        point_id,
        point_name,
        iot_device_id,
        iot_device_name,
        value,
        unit,
        up_region_id,
        create_time,
        magnification_coefficient,
        edge_code,
        iot_device_type
        ) values
        <foreach collection="list" separator="," item="item">
            (
            #{item.id, jdbcType=BIGINT},
            #{item.pointId, jdbcType=BIGINT},
            #{item.pointName, jdbcType=VARCHAR},
            #{item.iotDeviceId, jdbcType=BIGINT},
            #{item.iotDeviceName, jdbcType=VARCHAR},
            #{item.value, jdbcType=VARCHAR},
            #{item.unit, jdbcType=VARCHAR},
            #{item.upRegionId, jdbcType=BIGINT},
            #{item.createTime, jdbcType=TIMESTAMP},
            #{item.magnificationCoefficient, jdbcType=FLOAT},
            #{item.edgeCode, jdbcType=VARCHAR},
            #{item.iotDeviceType, jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <select id="getSystemConfig" resultType="java.lang.String">
        SELECT config_value FROM system_config
            WHERE config_type =#{configType, jdbcType=VARCHAR} and config_key =#{configKey, jdbcType=VARCHAR}
            limit 1
    </select>
</mapper>
