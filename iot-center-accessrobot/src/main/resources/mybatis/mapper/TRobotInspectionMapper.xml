<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjh.accessrobot.module.command.dao.TRobotInspectionDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yjh.accessrobot.module.command.entity.TRobotInspection">
        <id column="inspection_id" jdbcType="BIGINT" property="inspectionId" />
        <result column="inspection_code" jdbcType="VARCHAR" property="inspectionCode" />
        <result column="robot_id" jdbcType="BIGINT" property="robotId" />
        <result column="inspection_name" jdbcType="VARCHAR" property="inspectionName" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        inspection_id, inspection_code,robot_id, inspection_name, inspection_type, alarm_top, alarm_bottom, default_value, inspection_position, collect_status, calibration_status, unit
    </sql>


    <insert id="insert" useGeneratedKeys="true" keyProperty="inspectionId" parameterType="com.yjh.accessrobot.module.command.entity.TRobotInspection">
        insert into t_robot_inspection(
            inspection_id,
            inspection_code,
            robot_id,
            inspection_name,
            component_id,
            meter_type,
            appearance_type,
            save_type_list,
            recognition_type_list,
            phase,
            device_info
        )values(
                #{inspectionId, jdbcType=BIGINT},
                #{inspectionCode, jdbcType=VARCHAR},
                #{robotId, jdbcType=BIGINT},
                #{inspectionName, jdbcType=VARCHAR},
                #{componentId, jdbcType=VARCHAR},
                #{meterType, jdbcType=INTEGER},
                #{appearanceType, jdbcType=INTEGER},
                #{saveTypeList, jdbcType=VARCHAR},
                #{recognitionTypeList, jdbcType=VARCHAR},
                #{phase, jdbcType=VARCHAR},
                #{deviceInfo, jdbcType=VARCHAR}
        )
    </insert>

    <delete id="deleteByPrimaryId" parameterType="java.lang.Long">
        delete from t_robot_inspection
                where inspection_id = #{inspectionId, jdbcType=BIGINT}
    </delete>

    <update id="update" parameterType="com.yjh.accessrobot.module.command.entity.TRobotInspection">
        update t_robot_inspection a <set>
        <if test="inspectionCode != null "> a.inspection_code = #{inspectionCode},</if>
        <if test="robotId != null ">a.robot_id = #{robotId},</if>
        <if test="inspectionName != null ">a.inspection_name = #{inspectionName},</if>
        <if test="componentId != null ">a.component_id = #{componentId},</if>
        <if test="meterType != null ">a.meter_type = #{meterType, jdbcType=INTEGER},</if>
        <if test="appearanceType != null ">a.appearance_type = #{appearanceType, jdbcType=INTEGER},</if>
        <if test="saveTypeList != null ">a.save_type_list = #{saveTypeList},</if>
        <if test="recognitionTypeList != null ">a.recognition_type_list = #{recognitionTypeList},</if>
        <if test="phase != null ">a.phase = #{phase},</if>
        <if test="deviceInfo != null ">a.device_info = #{deviceInfo},</if>

    </set>
        where a.inspection_code = #{inspectionCode, jdbcType=VARCHAR}
    </update>

    <select id="selectByPrimaryId" parameterType="java.lang.Long" resultType="com.yjh.accessrobot.module.command.entity.TRobotInspection">
        select
            a.inspection_id,
            a.inspection_code,
            a.robot_id,
            a.inspection_name,
            a.component_id,
            a.meter_type,
            a.appearance_type,
            a.save_type_list,
            a.recognition_type_list,
            a.phase,
            a.device_info
        from t_robot_inspection a
        where 1 = 1
            and a.inspection_id = #{inspectionId, jdbcType=BIGINT}
        limit 1
    </select>

    <select id="select" parameterType="com.yjh.accessrobot.module.command.entity.TRobotInspection" resultType="com.yjh.accessrobot.module.command.entity.TRobotInspection">
        select
        a.inspection_id,
        a.inspection_code,
        a.robot_id,
        a.inspection_name,
        a.component_id,
        a.meter_type,
        a.appearance_type,
        a.save_type_list,
        a.recognition_type_list,
        a.phase,
        a.device_info
        from t_robot_inspection a
        where 1 = 1
        <if test="inspectionId != null and inspectionId != ''"> and a.inspection_id = #{inspectionId, jdbcType=BIGINT}</if>
        <if test="inspectionCode != null and inspectionCode != ''"> and a.inspection_code = #{inspectionCode, jdbcType=VARCHAR}</if>
        <if test="robotId != null and robotId != ''"> and a.robot_id = #{robotId, jdbcType=BIGINT}</if>
        <if test="inspectionName != null and inspectionName != ''"> and a.inspection_name = #{inspectionName, jdbcType=VARCHAR}</if>
        <if test="componentId != null and componentId != ''">a.component_id = #{componentId, jdbcType=VARCHAR},</if>
        <if test="meterType != null ">a.meter_type = #{meterType, jdbcType=Integer},</if>
        <if test="appearanceType != null ">a.appearance_type = #{appearanceType, jdbcType=Integer},</if>
        <if test="saveTypeList != null and saveTypeList != ''">a.save_type_list = #{saveTypeList, jdbcType=VARCHAR},</if>
        <if test="recognitionTypeList != null and recognitionTypeList != ''">a.recognition_type_list = #{recognitionTypeList, jdbcType=VARCHAR},</if>
        <if test="phase != null and phase != ''">a.phase = #{phase, jdbcType=VARCHAR},</if>
        <if test="deviceInfo != null and deviceInfo != ''">a.device_info = #{deviceInfo, jdbcType=VARCHAR},</if>
    </select>

    <select id="selectByPage" parameterType="com.yjh.accessrobot.module.command.entity.TRobotInspection" resultType="com.yjh.accessrobot.module.command.entity.TRobotInspection">
        select
        a.inspection_id,
        a.inspection_code,
        a.robot_id,
        a.inspection_name,
        a.inspection_type,
        a.alarm_top,
        a.alarm_bottom,
        a.default_value,
        a.inspection_position,
        a.collect_status,
        a.calibration_status,
        a.unit
        from t_robot_inspection a
        where 1 = 1
        <if test="inspectionId != null and inspectionId != ''"> and a.inspection_id = #{inspectionId, jdbcType=BIGINT}</if>
        <if test="inspectionCode != null and inspectionCode != ''"> and a.inspection_code = #{inspectionCode, jdbcType=VARCHAR}</if>
        <if test="robotId != null and robotId != ''"> and a.robot_id = #{robotId, jdbcType=BIGINT}</if>
        <if test="inspectionName != null and inspectionName != ''"> and a.inspection_name = #{inspectionName, jdbcType=VARCHAR}</if>
        <if test="inspectionType != null and inspectionType != ''"> and a.inspection_type = #{inspectionType, jdbcType=INTEGER}</if>
        <if test="alarmTop != null and alarmTop != ''"> and a.alarm_top = #{alarmTop, jdbcType=VARCHAR}</if>
        <if test="alarmBottom != null and alarmBottom != ''"> and a.alarm_bottom = #{alarmBottom, jdbcType=VARCHAR}</if>
        <if test="defaultValue != null and defaultValue != ''"> and a.default_value = #{defaultValue, jdbcType=VARCHAR}</if>
        <if test="inspectionPosition != null and inspectionPosition != ''"> and a.inspection_position = #{inspectionPosition, jdbcType=INTEGER}</if>
        <if test="collectStatus != null and collectStatus != ''"> and a.collect_status = #{collectStatus, jdbcType=INTEGER}</if>
        <if test="calibrationStatus != null and calibrationStatus != ''"> and a.calibration_status = #{calibrationStatus, jdbcType=INTEGER}</if>
        <if test="unit != null and unit != ''"> and a.unit = #{unit, jdbcType=VARCHAR}</if>
    </select>

    <!--机器人测点表中现有的点-->
    <select id="selectAllByRobotId" resultType="java.lang.String">
        SELECT
        a.inspection_code
        FROM
        t_robot_inspection a
        WHERE 1 = 1
        <if test="robotId != null and robotId != ''"> and a.robot_id = #{robotId, jdbcType=BIGINT}</if>
    </select>
    <!--根据测点编码list查询测点list-->
    <select id="selectInspectionIdList" resultType="java.lang.Long">
        SELECT
        inspection_id
        FROM
        t_robot_inspection
        WHERE 1 = 1 and
        inspection_code IN
        <foreach collection="list" item="inspectionCode" open="(" separator="," close=")">
            #{inspectionCode, jdbcType=VARCHAR}
        </foreach>
    </select>
    <!--根据测点list查询巡检点list-->
    <select id="selectInstanceIdList" resultType="java.lang.Long">
        SELECT
        instance_id
        FROM
        t_cruise_point_instance
        WHERE 1 = 1 and
        cruise_id IN
        <foreach collection="list" item="inspectionId" open="(" separator="," close=")">
            #{inspectionId, jdbcType=BIGINT}
        </foreach>
    </select>
    <!--批量删除机器人测点表数据-->
    <delete id="batchDeleteTRobotInspection">
        DELETE
        FROM
        t_robot_inspection
        WHERE
        inspection_id IN
        <foreach collection="list" item="inspectionId" open="(" separator="," close=")">
            #{inspectionId, jdbcType=VARCHAR}
        </foreach>
    </delete>
    <!--批量删除巡检点表数据-->
    <delete id="batchDeleteTCruisePointInstance">
        DELETE
        FROM
        t_cruise_point_instance
        WHERE
        instance_id IN
        <foreach collection="list" item="instanceId" open="(" separator="," close=")">
            #{instanceId, jdbcType=BIGINT}
        </foreach>
    </delete>
    <!--批量删除巡检预案属性表数据-->
    <delete id="batchDeleteTCruisePlanAttr">
        DELETE
        FROM
        t_cruise_plan_attr
        WHERE
        instance_id IN
        <foreach collection="list" item="instanceId" open="(" separator="," close=")">
            #{instanceId, jdbcType=BIGINT}
        </foreach>
    </delete>
    <!--批量插入机器人测点表数据-->
    <insert id="batchInsertTRobotInspection">
        insert into t_robot_inspection(
        inspection_id,
        inspection_code,
        robot_id,
        inspection_name,
        component_id,
        meter_type,
        appearance_type,
        save_type_list,
        recognition_type_list,
        phase,
        device_info
        )values
        <foreach collection="list" item="TRobotInspection" separator=",">
            (
            #{TRobotInspection.inspectionId, jdbcType=BIGINT},
            #{TRobotInspection.inspectionCode, jdbcType=VARCHAR},
            #{TRobotInspection.robotId, jdbcType=BIGINT},
            #{TRobotInspection.inspectionName, jdbcType=VARCHAR},
            #{TRobotInspection.componentId, jdbcType=VARCHAR},
            #{TRobotInspection.meterType, jdbcType=INTEGER},
            #{TRobotInspection.appearanceType, jdbcType=INTEGER},
            #{TRobotInspection.saveTypeList, jdbcType=VARCHAR},
            #{TRobotInspection.recognitionTypeList, jdbcType=VARCHAR},
            #{TRobotInspection.phase, jdbcType=VARCHAR},
            #{TRobotInspection.deviceInfo, jdbcType=VARCHAR}
            )
        </foreach>
    </insert>
    <select id="selectInspection" parameterType="java.lang.String" resultType="java.util.Map">
        SELECT
            a.inspection_id,
            a.inspection_name
        FROM
            t_robot_inspection a
        WHERE 1 = 1
        <if test="inspectionCode != null and inspectionCode != ''"> and a.inspection_code = #{inspectionCode, jdbcType=VARCHAR}</if>
    </select>


</mapper>
